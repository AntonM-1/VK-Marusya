export interface Movie {
    id: number
    title: string
    releaseYear: number
    genres: string[]
    runtime: number
    tmdbRating: number
    plot: string
    backdropUrl: string
    posterUrl: string
    trailerUrl: string
}